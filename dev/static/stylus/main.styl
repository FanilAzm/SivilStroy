// Подключаем стили библиотек
@import '_libs.styl'

// Подключаем переменные
@import '_vars.styl'

//Подключаем спрайты (просто раскоментируем импорт при появлении PNG спрайтов)
@import '_sprite.styl'

// Подключаем миксины
@import '_mixins.styl'

html 
  line-height 1
  font-size 16px

body
  font-family mainFont
  color mainTextColor
  &.popup-open
    overflow hidden

h1,h2,h3,h4,h5,h6
  margin 0

ul,li
  margin 0
  padding 0
  list-style-type none
  
p
  margin 0
  
a
  text-decoration none
  color black

audio,
canvas,
iframe,
img,
svg,
video
  vertical-align middle
  
textarea
  resize none

section
  position relative

input,
select,
button
  outline none

*, *:before, *:after
  box-sizing border-box
  
.hide
  display none
  
.show
  display block
  
::-moz-selection 
  background: #b3d4fc;
  text-shadow: none;
  
::selection 
  background: #b3d4fc;
  text-shadow: none;
  
.clearfix:before,
.clearfix:after 
  content: " ";
  display: table;

.clearfix:after 
  clear: both;

.clearfix 
  *zoom: 1;

.container
  position relative
  width 97%
  max-width 1562px
  margin 0 auto
  
.content
  position relative
  flex 1 0 auto
  
.wrapper
  // min-height calc(100vh - 80px)
  display flex
  flex-direction column
  height 100%



// Общие стили
@import '_common_styles.styl'

// Стили шапки
@import 'modules/_header.styl'

// Стили меню
@import 'modules/_menu.styl'

// Стили всплывающего окна
@import 'modules/_popup.styl'

// Стили главной страницы
@import 'page/_index.styl'

// Стили страницы системы контроля
@import 'page/_control.styl'

// Стили страницы пожарные системы
@import 'page/_fire-system.styl'

// Стили страницы охранной системы
@import 'page/_security-system.styl'

// Стили страницы кабельные сети
@import 'page/_cable-networks.styl'

// Стили страницы видео для дома
@import 'page/_video-home.styl'

// Стили страницы охрана для дома
@import 'page/_security-home.styl'

// Стили страницы проектов
@import 'page/_projects.styl'

// Стили футера
@import 'modules/_footer.styl'

// Стили медиазапросов
@import '_media.styl'